import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false,  // This line disables the Next.js logo in the bottom left corner
};

export default nextConfig;
